// 'div'로 지정된 태그 이름으로 div 엘리먼트를 생성 => <div></div>
document.createElement('div');
// 브라우저 내부 메모리에 변수 div를 저장
var element_div = document.createElement('div');
// div태그에 id를 추가
element_div.id = 'div_name';