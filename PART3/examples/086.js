// 배열 마지막 요소 추출하기 (pop)
const arr = [1, 2, 3];

console.log(arr.pop());
console.log(arr.pop());
console.log(arr.pop());
console.log(arr.pop());                     // 배열 요소 개수보다 많이 호출되면 undefined를 반환함, [1, 2, 3]의 pop() 호출 결과는 3 -> 2 -> 1 순서로 추출