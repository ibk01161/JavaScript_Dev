// 배열 뒤에 요소 추가하기 (push)
const festa = ['mang'];

// 문장 순서대로 festa 배열 뒤에 요소를 추가
festa.push('chimmy');
festa.push('tata');
festa.push('cooky');
festa.push('shooky');
festa.push('koya');
festa.push('rj');

festa.forEach(name => {
    console.log(name);
});